<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright (c) 2026 chenxinjie

  SPDX-License-Identifier: MIT
-->

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cn.chenxinjie</groupId>
        <artifactId>upload-file</artifactId>
        <version>1.0.0-rc.5</version>
    </parent>

    <artifactId>upload-file-spring-boot-starter-jakarta</artifactId>
    <name>upload-file-spring-boot-starter-jakarta</name>
    <description>Spring Boot 3/4 (jakarta) auto-configuration: drop-in twin of upload-file-spring-boot-starter; auto-wires core services and registers upload/download servlets</description>

    <url>https://github.com/FoamValue/upload-file</url>
    <scm>
        <connection>scm:git:git@github.com:FoamValue/upload-file.git</connection>
        <developerConnection>scm:git:git@github.com:FoamValue/upload-file.git</developerConnection>
        <url>https://github.com/FoamValue/upload-file</url>
    </scm>

    <properties>
        <!-- Import the Spring Boot 4 BOM module-locally so the jakarta modules resolve the
             Boot 4 spring-boot(-autoconfigure) / jakarta.servlet-api / test-starter versions;
             overrides the parent Boot 2.7 BOM. Version property inherited from the parent pom
             (spring.boot4.version); bytecode target stays at the parent's release 8. -->
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot4.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>cn.chenxinjie</groupId>
            <artifactId>upload-file-core</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.chenxinjie</groupId>
            <artifactId>upload-file-servlet-jakarta</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.chenxinjie</groupId>
            <artifactId>upload-file-store-jdbc</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>cn.chenxinjie</groupId>
            <artifactId>upload-file-store-redis</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
                <extensions>true</extensions>
                <configuration>
                    <publishingServerId>central</publishingServerId>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
